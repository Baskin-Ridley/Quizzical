const initialState = {
    question_category: "",
    question_type: "",
    question_difficulty: "",
    amount_of_questions: 5,
    score: 0
}

const reducer = (state = initialState, action: any) => {
    return state;
}

export default reducer;